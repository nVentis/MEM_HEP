class MEMIntegrand
{
    private:
        /* data */
    public:
        MEMIntegrand(/* args */);
        MEMIntegrand(MEMIntegrand const&)            = delete;
        MEMIntegrand& operator=(MEMIntegrand const&) = delete;
        ~MEMIntegrand();
};

MEMIntegrand::MEMIntegrand(/* args */)
{
}

MEMIntegrand::~MEMIntegrand()
{
}
